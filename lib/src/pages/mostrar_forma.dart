import 'package:flutter/material.dart';


class MostrarFigura extends StatelessWidget {
  const MostrarFigura({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Figura Seleccioanda'),),
      body: Container(
        child: Text('hola'),
      ),
    );
  }
}